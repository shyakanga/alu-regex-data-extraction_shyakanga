# Regex Data Extraction

To run the project, use `python3 main.py` from the root directory of this project.

It uses functions containing regex to extract matching text, store it in the `output` folder in JSON format, then display them in lists.
